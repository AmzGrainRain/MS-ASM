DSEG SEGMENT
  MSG DB "HELLO WORLD", 0DH, 0AH, 24H
  I DW ?
  MAX DW ?
DSEG ENDS

SSEG SEGMENT
SSEG ENDS

CSEG SEGMENT
  ASSUME DS:DSEG, SS:SSEG, CS:CSEG
  MAIN:
    MOV AX, DSEG
    MOV DS, AX

    MOV AX, I
    MOV AX, 0H
    MOV I, AX
    MOV AX, MAX
    MOV AX, 5H
    MOV MAX, AX

  LOOP_1:
    MOV AX, I
    CMP AX, MAX
    JE CONTIUNE_RUN
    INC AX
    MOV I, AX
    MOV DX, OFFSET MSG
    MOV AH, 9
    INT 21H
    JMP LOOP_1
  
  CONTIUNE_RUN:
    MOV AX, 4CH
    INT 21H
CSEG ENDS
END MAIN
