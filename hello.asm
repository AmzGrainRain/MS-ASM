DSEG SEGMENT
  STR DB "HELLO WORLD!", 0DH, 0AH, 24H
DSEG ENDS

SSEG SEGMENT
SSEG ENDS

CSEG SEGMENT
  ASSUME DS:DSEG, SS:SSEG, CS:CSEG
  BEGIN:
  MOV AX, DSEG
  MOV DS, AX
  MOV DX, OFFSET STR
  MOV AH, 9
  INT 21H
  MOV AH, 4CH
  INT 21H
CSEG ENDS
END BEGIN
